# Application
# The environment the app is running in (development, test, production)
NODE_ENV=development

# Database
# SQLite database URL - relative to project root
DATABASE_URL="file:./data/app.db"

# Authentication
# Secret key for JWT token generation - CHANGE THIS IN PRODUCTION!
JWT_SECRET="change-this-in-production"

# Optional Configuration
# The base URL where your application is hosted (optional, defaults to request origin)
# APP_URL=http://localhost:3000

# JWT token expiration time (optional, defaults to "24h")
# JWT_EXPIRES_IN="7d"

# Name of the session cookie (optional, defaults to "auth_token")
# SESSION_COOKIE_NAME="cc_session"

# Directory for database backups - relative to project root (optional, defaults to "./data/backups")
# DATABASE_BACKUP_DIR="./data/backups"